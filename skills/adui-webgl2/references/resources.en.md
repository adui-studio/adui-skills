# WebGL2 Resources and Lifecycle

English fallback for the Chinese source of truth.

Assign explicit owners for buffers, textures, samplers, framebuffers, renderbuffers, VAOs, shaders, programs, queries, and transform feedback objects. Provide a deterministic disposal path. Recreate context-bound GPU resources after context restoration. Treat readPixels and other readbacks as synchronization points that require measurement.
