# Testing and Migrations

English fallback for `testing-migrations.md`.

Use unit tests for application branches, real database integration tests for Prisma queries, constraints, and transactions, and API integration/E2E tests for NestJS transport behavior. For schema changes, validate configuration, generate the client, build/type-check NestJS, review generated migration SQL, and assess existing data, locks, backfills, compatibility windows, and rollback options.
