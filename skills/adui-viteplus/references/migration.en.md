# Migrating to Vite+

English fallback for `migration.md`.

Inventory the existing toolchain and every CI, hook, IDE, Docker, release, and documentation reference. Migrate one capability at a time, verify equivalent behavior, then remove obsolete dependencies and configuration only after references are gone.

Do not combine a Vite+ migration with unrelated package-manager, Node, or architecture migrations unless explicitly required.
